# Dependencies
node_modules/

# Build output
dist/

# CocoaPods (restored on the CI runner via `pod install`)
ios/App/Pods/
ios/App/Podfile.lock

# Xcode user data
ios/App/App.xcodeproj/xcuserdata/
ios/App/App.xcworkspace/xcuserdata/
*.xcuserstate
ios/App/build/
DerivedData/

# macOS
.DS_Store

# Logs
*.log
npm-debug.log*

# Signing artifacts — NEVER commit these
*.p12
*.cer
*.pem
*.key
*.certSigningRequest
*.mobileprovision
*.p8
.abacus.donotdelete
.github/workflows/build-ios.yml

# ===== Android =====
# Build outputs
android/app/build/
android/build/
android/.gradle/
android/capacitor-cordova-android-plugins/build/
*.apk
*.aab
# Signing secrets — NEVER commit these
android/key.properties
*.jks
*.keystore
keystore_base64.txt
# Firebase config (contains project keys; add via CI or locally)
android/app/google-services.json
# Local SDK location
android/local.properties
